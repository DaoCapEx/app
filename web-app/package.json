{
  "name": "app",
  "version": "0.1.0",
  "private": false,
  "dependencies": {
    "@metamask/onboarding": "^1.0.1",
    "axios": "^0.24.0",
    "bootstrap": "^5.1.0",
    "chart.js": "^2.9.4",
    "feather-icons-react": "^0.5.0",
    "metismenujs": "^1.3.1",
    "react": "^17.0.2",
    "react-router-dom": "^6.1.0",
    "react-scripts": "^5.0.0",
    "reactstrap": "^9.0.1",
    "sass": "^1.45.0",
    "simplebar-react": "^2.3.6",
    "universal-cookie": "^4.0.4",
    "web3": "^1.6.1"
  },
  "scripts": {
    "dev": "ESLINT_NO_DEV_ERRORS=true NODE_ENV=development SKIP_PREFLIGHT_CHECK=true PORT=2008 react-scripts --openssl-legacy-provider start",
    "build": "SKIP_PREFLIGHT_CHECK=true react-scripts --openssl-legacy-provider build",
    "test": "react-scripts --openssl-legacy-provider test",
    "eject": "react-scripts --openssl-legacy-provider eject",
    "start": "NODE_ENV=production PORT=2008 serve -s build"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "dotenv-webpack": "^7.0.3",
    "jest": "^27.4.5"
  }
}
